# 兰台隐私说明

生效日期：2026-09-21

兰台是一款在用户自己的 SillyTavern 环境中运行的第三方扩展。兰台没有开发者运营的数据服务器，不包含广告、遥测、使用统计或独立的联网版本检查。

## 本地保存的数据

兰台会通过 SillyTavern 提供的存储能力保存以下内容：

- 插件级设置、提示词配置、全局模块及相关结果保存在 SillyTavern 的扩展设置中。
- 当前聊天的记忆、总结批次、故事时间、日历、纪念日和聊天模块保存在对应聊天的元数据中。
- 可由权威数据重新生成的派生索引可能保存在当前浏览器的 IndexedDB 中。
- 插件 API Key 只写入 SillyTavern 后端 Secrets；插件设置、聊天数据和导出文件只保存 Secret ID，不保存 API Key 明文。

这些数据由用户自己的 SillyTavern 实例和浏览器管理。停用兰台只会停止相应功能，不会自动删除已有数据；卸载扩展也不应被视为已经清除所有设置、聊天元数据、Secrets 或浏览器缓存。

## 发送给 AI 服务的数据

只有在用户主动执行需要 AI 的操作，或已经启用自动总结等自动任务时，兰台才会发起生成请求。请求可能包含：

- 完成当前任务所需的兰台提示词和结构化输出约束；
- 用户为该任务选择的聊天楼层，经已启用的文本清洗规则处理后的内容；
- 完成关键词、简称、合并、记忆维护或模块任务所需的相关插件数据。

数据会发送至用户明确选择的来源：

- **插件 API**：发送至用户配置的 API 地址与模型，由 SillyTavern 后端使用保存于 Secrets 中的密钥转发。
- **SillyTavern 当前 API**：通过 SillyTavern 当前连接发送。

兰台不会在插件 API 缺失或失败时静默改用 SillyTavern 当前 API。AI 服务如何处理请求数据，取决于用户选择的服务及其隐私政策。

## 其他网络连接

兰台运行时不会自行联系作者或其他统计服务。通过 GitHub 安装或更新扩展时产生的 GitHub 网络连接，由用户操作和 SillyTavern 的扩展管理功能发起。

## 日志

运行错误可能显示在当前 SillyTavern 页面或本地浏览器控制台中，但兰台不会把这些日志自动发送给作者。

向仓库提交问题时，请自行删除 API Key、私人聊天内容、角色资料及其他敏感信息。

## 用户控制

用户可以在兰台内停用插件或当前聊天功能、删除具有删除入口的记录，并使用 SillyTavern 提供的聊天、设置、Secrets 与浏览器数据管理方式进一步清理数据。执行不可恢复的清理前，请先按自己的 SillyTavern 备份方式保存重要内容。

本说明如随功能变化而更新，将记录在仓库版本历史中。
